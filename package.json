{
  "name": "2board",
  "repository": "https://github.com/vercel/examples.git",
  "license": "MIT",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "export NODE_ENV=development && nuxt dev --port 4000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "npx puppeteer browsers install chrome && nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.4.2",
    "turbo": "^1.9.3"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.9.2",
    "@vercel/postgres": "^0.1.3",
    "firebase": "^10.13.1",
    "js-sha256": "^0.11.0",
    "ms": "^2.1.3",
    "puppeteer": "^23.4.1",
    "sass-embedded": "^1.79.3",
    "ws": "^8.13.0"
  }
}